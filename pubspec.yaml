name: tilde_expansion
description: expand ~/ and ~user in paths
version: 1.1.0
repository: https://github.com/nohkumado/dart_tilde_expansion/

environment:
  sdk: ^3.5.0

# Add regular dependencies here.
dependencies:
   path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.8
